#define RF_ACTIVE         //RF need to be ON
#define NTP_ACTIVE        //enable to store real timestamp instead of millis
#define RF_EMITTER_PIN 2
#define RF_RECEIVER_PIN 0
#define RF_EMITTER_REPEAT 20
#define TIME_AVOID_DUPLICATE 3          // in secondss (must be at bigger than 1)
